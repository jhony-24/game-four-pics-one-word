import { StyleSheet } from "aphrodite/no-important"

const style = StyleSheet.create({
    flex: {
        justifyContent: "none",
        padding: "10px",
        width: "auto"
    },
    texticon: {
        fontWeight: "bold",
        userSelect: "none",
    }
})

export default style;
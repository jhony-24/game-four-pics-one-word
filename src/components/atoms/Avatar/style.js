import { StyleSheet } from "aphrodite/no-important"

const style = StyleSheet.create({
    avatar : {
        borderRadius : "100%"
    },
    ctnAvatar : {
        margin : "auto"
    }
})

export default style;